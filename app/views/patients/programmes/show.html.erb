<% content_for :navigation do %>
  <%= render AppBreadcrumbComponent.new(items: [
                                          { text: t("dashboard.index.title"), href: dashboard_path },
                                          { text: t("patients.index.title"), href: patients_path },
                                        ]) %>
<% end %>

<% content_for :page_title, "#{@patient.initials} – #{@programme.name}" %>
<%= render "patients/header" %>

<%= render AppPatientNavigationComponent.new(
      @patient,
      current_user.programmes,
      active: @programme.type.to_sym,
    ) %>

<%= render AppPatientProgrammeVaccinationComponent.new(
      @patient,
      @programme,
      academic_year: @academic_year,
    ) %>

<%= render AppPatientProgrammeSessionsComponent.new(
      @patient,
      @programme,
      academic_year: @academic_year,
      team: current_team,
    ) %>

<%= render AppPatientProgrammeActivityComponent.new(
      @patient,
      @programme,
      team: current_team,
    ) %>
